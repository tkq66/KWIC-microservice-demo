apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    run: chairwise-output-service
  name: chairwise-output-service
spec:
  replicas: 2
  selector:
    matchLabels:
      run: chairwise-output-service
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        run: chairwise-output-service
    spec:
      containers:
        - image: siddsatish95/chairwise-output-service:test
          name: chairwise-output-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
      dnsPolicy: ClusterFirst
      restartPolicy: Always